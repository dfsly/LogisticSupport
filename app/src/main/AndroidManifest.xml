<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dfsly.android.logisticsupport">

    <!--  悬浮球  -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!--  震动  -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_ls"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.dfsly.android.logisticsupport.MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:launchMode="singleTask" />
        <activity android:name=".AboutMeActivity" />

        <service android:name="com.dfsly.android.logisticsupport.LogisticService" />
    </application>

</manifest>